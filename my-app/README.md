# my-app

Scaffolded with [mern.dev](https://npmjs.com/package/mern.dev)

## Quick start

```bash
# 1. Fill in .env (MONGO_URI, JWT_SECRET)
# 2. Install deps
npm install
cd client && npm install

# 3. Run dev
npm run dev
```
